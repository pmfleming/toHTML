[package]
name = "tohtml"
version = "0.1.0"
edition = "2021"
description = "Local-first document to structured HTML converter"
license = "MIT"

[dependencies]
quick-xml = "0.40.1"
zip = { version = "8.6.0", default-features = false, features = ["deflate"] }
